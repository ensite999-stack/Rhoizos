import type {Metadata} from "next";
export const metadata:Metadata={title:"Domain Pricing | Rhoizos",description:"Transparent Rhoizos domain registration, renewal and transfer pricing."};
export default function Layout({children}:{children:React.ReactNode}){return children;}
