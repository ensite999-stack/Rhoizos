import type {Metadata} from "next";
export const metadata:Metadata={title:"Privacy Policy | Rhoizos",description:"How Rhoizos handles account, domain, DNS, payment and service data."};
export default function Layout({children}:{children:React.ReactNode}){return children;}
