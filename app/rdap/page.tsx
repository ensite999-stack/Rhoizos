"use client";import {FormEvent,useState} from "react";export default function Rdap(){const [result,setResult]=useState<unknown>(null),[error,setError]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");const f=new FormData(e.currentTarget),r=await fetch("/api/rdap?domain="+encodeURIComponent(String(f.get("domain")||""))),d=await r.json();if(!r.ok){setError(d.error||"Lookup failed.");return;}setResult(d);}return <div className="page"><p className="kicker">RDAP</p><h1 className="pageTitle">Public domain data.</h1><form className="searchForm form" onSubmit={submit}><input name="domain" placeholder="example.com" required/><button className="primary">Lookup</button></form>{error&&<p className="error">{error}</p>}{result!==null&&<pre className="rdapPre">{JSON.stringify(result,null,2)}</pre>}</div>}