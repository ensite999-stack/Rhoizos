"use client";import {FormEvent,useState} from "react";export default function Signup(){const [error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const body=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/auth/signup",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)}),d=await r.json();if(!r.ok){setError(d.error||"Signup failed.");setBusy(false);return;}location.href="/login";}return <div className="page"><p className="kicker">Account</p><h1 className="pageTitle">Create account.</h1><p className="pageIntro">The required contact details are used for domain registration and registrar operations.</p><form className="form" onSubmit={submit}><div className="formGrid"><label className="field">First name<input name="firstName" required/></label><label className="field">Last name<input name="lastName" required/></label><label className="field">Email<input name="email" type="email" required/></label><label className="field">Password<input name="password" type="password" minLength={12} required/></label><label className="field">Account type<select name="accountType"><option value="individual">Individual</option><option value="company">Company</option></select></label><label className="field">Company<input name="company"/></label><label className="field">Phone<input name="phone" placeholder="+86.13800138000" required/></label><label className="field">Country code<input name="country" placeholder="SG" maxLength={2} required/></label><label className="field">State / province<input name="state" required/></label><label className="field">City<input name="city" required/></label><label className="field full">Street address<input name="address1" required/></label><label className="field">Postal / ZIP<input name="postcode" required/></label></div><p className="fine">Use Latin characters for registry contact fields.</p>{error&&<p className="error">{error}</p>}<button className="primary" disabled={busy}>Create account</button></form></div>}