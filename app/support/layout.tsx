import type {Metadata} from "next";
export const metadata:Metadata={title:"Support | Rhoizos",description:"Contact Rhoizos for account help, domain operations, abuse reports and general support."};
export default function Layout({children}:{children:React.ReactNode}){return children;}
