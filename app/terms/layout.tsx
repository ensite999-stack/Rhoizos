import type {Metadata} from "next";
export const metadata:Metadata={title:"Terms of Service | Rhoizos",description:"Terms for Rhoizos domain registration, transfer, renewal, payment and DNS services."};
export default function Layout({children}:{children:React.ReactNode}){return children;}
