import type {Metadata} from "next";
export const metadata:Metadata={title:"Domain Transfer | Rhoizos",description:"Transfer an eligible domain to Rhoizos with its EPP/Auth Code, clear transfer pricing and status tracking."};
export default function Layout({children}:{children:React.ReactNode}){return children;}
